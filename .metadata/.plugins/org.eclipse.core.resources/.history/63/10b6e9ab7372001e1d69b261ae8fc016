<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Rumah Sakit</title>
    <style>
       .topnav {
            background-color: #333;
            overflow: hidden;
        }

        /* Style the links inside the navigation bar */
        .topnav a {
            float: left;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 17px;
        }

        /* Change the color of links on hover */
        .topnav a:hover {
            background-color: #ddd;
            color: black;
        }

        /* Add a color to the active/current link */
        .topnav a.active {
            background-color: #04AA6D;
            color: white;
        }
    </style>
    <!-- Include your CSS file here -->
    <!-- Example: <link rel="stylesheet" type="text/css" href="path_to_your_css_file.css"> -->
</head>
<body>
    <div class="topnav">
        <a class="active" th:href="@{/dashboard}">Beranda</a>
        <a th:href="@{/kamars/list}">Kamar</a>
        <a href="">Daftar Pasien</a>
        <a th:href="@{/logout}">Logout</a>
    </div>
    <h1>Add New Kamar</h1>
    <form th:action="@{/kamars/add}" method="POST" th:object="${kamar}">
        <label for="id">ID:</label>
        <input type="text" id="id" th:field="*{id}" name="id" required><br><br>

        <label for="nomor_kamar">Nomor Kamar:</label>
        <input type="text" id="nomor_kamar" th:field="*{nomor_kamar}" name="nomor_kamar" required><br><br>

        <label for="jenis_kamar">Jenis Kamar:</label>
        <input type="text" id="jenis_kamar" th:field="*{jenis_kamar}" name="jenis_kamar" required><br><br>

        <label for="kapasitas">Kapasitas:</label>
        <input type="text" id="kapasitas" th:field="*{kapasitas}" name="kapasitas" required><br><br>

        <label for="harga">Harga:</label>
        <input type="text" id="harga" th:field="*{harga}" name="harga" required><br><br>

        <button type="submit">Add</button>
    </form>
</body>
</html>
